<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.ClemenTime" parent="Theme.AppCompat.Light.NoActionBar" />

    <!-- Starting theme for the Splash Screen -->
    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/launcher_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.ClemenTime</item>
    </style>

</resources>